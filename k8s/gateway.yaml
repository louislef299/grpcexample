apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: ping-grpc-gwy
  namespace: lefebl4
spec:
  selector:
    app: istio-ingressgateway
  servers:
  - port:
      number: 443
      name: https-ping
      protocol: HTTPS
    tls:
      mode: SIMPLE
      credentialName: ping-example
    hosts:
    - "ping.dev2.mlife"
